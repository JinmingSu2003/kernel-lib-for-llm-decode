[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "triton-decode-lab"
version = "0.0.1"
description = "Scaffold for a Triton LLM decode optimization project; kernels not implemented yet"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
